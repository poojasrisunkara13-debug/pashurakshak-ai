//find min of a and b
#include<stdio.h> 
int main()
int result=((a<b)?a:b);
while (result>0)
{
	if (a% result==0&&b%result==0)
	{ 
	break;
	}
	result;
	}
	int main()
	{ 
	printf("enter 2 values:");
	scanf("%d%d",&a,&b);
	printf("gcd of %d and %d",a,b,gcd(a,b));
	}
}
